You are helping me build a single-user Python stock/options trading API.

Project name:
stocks-api

Repo:
https://github.com/misterhedrick/stocks-api.git

Hosted URL:
https://stocks-api-z11i.onrender.com/

Current local workspace:
c:\Users\Miste\OneDrive\Documents\dev\stocks-api

Last repo/app scope review:
2026-05-02

High-level project goal:
- Build a FastAPI backend for one user.
- Use Alpaca for broker access and options market data.
- Start with paper trading first.
- Keep the design live-trading-ready, but do not optimize for live trading yet.
- Use Postgres as durable state.
- Keep the architecture simple, practical, and production-minded.

Core architecture decisions:
- Framework: FastAPI.
- Database: Postgres.
- ORM/migrations: SQLAlchemy 2.x and Alembic.
- Broker/data source: Alpaca.
- Hosting: Render.
- Auth: single admin bearer token/API token from environment variables.
- Deployment style: free-first now, but paid-ready later.
- Execution model: trades should flow through previewed order_intents before execution.
- Source of truth for orders, fills, and positions: Alpaca.
- Longer term, execution and reconciliation should move to cron/worker-style jobs.

Current deployed state:
- Render public app is live at https://stocks-api-z11i.onrender.com/.
- Confirmed public endpoints on 2026-04-24:
  - GET / returns {"message":"stocks-api is running"}.
  - GET /health returns {"status":"ok"}.
  - GET /api/v1/health returns {"status":"ok"}.
  - GET /api/v1/order-intents returns 401 without bearer auth, as expected.
  - GET /openapi.json is exposed and shows current FastAPI routes.
- The private Render dashboard requires a logged-in browser/session, so Codex cannot inspect private service settings directly.
- render.yaml is the best local source of deployment intent.
- Render service config in render.yaml:
  - service type: web
  - name: stocks-api
  - env: python
  - plan: free
  - buildCommand: pip install -r requirements.txt
  - startCommand: bash ./start.sh
  - healthCheckPath: /health
  - expected env vars: DATABASE_URL, ADMIN_API_TOKEN, ALPACA_API_KEY, ALPACA_API_SECRET
  - APP_ENV=production
  - AUTO_MIGRATE_ON_STARTUP=true
  - ALPACA_PAPER=true

Important local/network note:
- On Windows, curl/PowerShell may fail against Render with certificate revocation errors.
- Working deployed checks used:
  curl.exe --ssl-no-revoke -L --max-time 90 https://stocks-api-z11i.onrender.com/health
- Do not treat the initial revocation-check failure as an app outage.

Current repo structure and purpose:
- README.md
  - Local setup, Render notes, health/readiness examples, order intent examples.
- ai_refresh.txt
  - This handoff file. Keep it updated when project scope changes.
- strategy_refresh.txt
  - Strategy-improvement-focused handoff. Use this when the task is strategy tuning rather than app development.
- env.example
  - Environment variable template.
- requirements.txt
  - Runtime dependencies: FastAPI, Uvicorn, SQLAlchemy, Alembic, Pydantic, httpx, psycopg.
- runtime.txt
  - Python runtime for hosting.
- render.yaml
  - Render service blueprint.
  - Includes web service and disabled-by-default market-cycle cron service.
- start.sh
  - Render/Linux startup script. Checks database connectivity, then starts uvicorn.
- run-local.ps1
  - Windows PowerShell local startup script. Activates .venv, checks DB, applies migrations, starts uvicorn.
- run-local.sh
  - Git Bash local startup script. Activates .venv, applies migrations, starts start.sh with --reload.
- postman/
  - Postman collection/environment for root, health, ready, create/list/submit order intent.
- alembic/
  - Alembic migration setup.
  - 0001_initial_trading_tables.py: strategies, signals, order_intents, broker_orders, fills, position_snapshots, job_runs, audit_logs.
  - 0002_add_performance_indexes.py: 13 query-performance indexes on signals, order_intents, broker_orders, fills, position_snapshots, job_runs.
  - 0003_add_ai_review_tables.py: trade_cases, ai_trade_reviews, strategy_change_suggestions tables with FKs and indexes.
- app/main.py
  - FastAPI app entrypoint.
  - Adds root and /health.
  - Includes /api/v1 automation, health, jobs, order-intents, options, signals, and strategies routers.
  - Runs Alembic migrations on startup if settings.should_auto_migrate_on_startup is true.
- app/core/config.py
  - Pydantic settings loaded from env/.env.
  - Normalizes Render postgres:// URLs to postgresql+psycopg://.
  - Picks Alpaca paper/live base URL from ALPACA_PAPER.
  - Provides Alpaca market data base URL for option quote previews.
  - Determines startup auto-migration behavior.
  - Provides market-cycle feature switches for scan/reconcile/preview/exit/news/submit.
  - Provides configurable RSS settings for market and owned-ticker news checks.
- app/core/security.py
  - Single admin bearer token dependency.
- app/db/base.py
  - SQLAlchemy DeclarativeBase.
- app/db/models.py
  - SQLAlchemy models for all trading persistence tables.
  - Includes TradeCase, AiTradeReview, and StrategyChangeSuggestion models added in 0003.
- app/db/session.py
  - SQLAlchemy engine/session setup.
  - DB connection check and schema readiness check.
- app/db/migrations.py
  - Programmatic Alembic upgrade helper used by FastAPI startup.
- app/db/check_database.py
  - CLI database connectivity check used by startup scripts.
- scripts/run_render_job.py
  - Render cron helper that POSTs a protected API job endpoint.
  - Exits without calling the API unless SCHEDULED_JOBS_ENABLED is truthy.
  - Retries retryable HTTP responses 429/500/502/503/504 using JOB_RETRY_DELAYS_SECONDS, default 10,30.
- scripts/seed_paper_strategies.py
  - Seeds preview-first paper strategies into the configured database.
  - Builds SPY/QQQ strategy configs from latest IEX quote midpoints.
  - Enables scanner-driven previews and keeps scanner.submit.enabled=false.
  - Supports --dry-run, --sample-prices, and --deactivate-others.
- scripts/tune_paper_strategies.py
  - Lists active strategy scanner/preview/submit state.
  - Creates or updates one preview-first moving-average paper strategy.
  - Creates or updates one preview-first confirmed-trend paper strategy.
  - Deep-merges JSON scanner config patches by strategy name and supports direct scanner patch flags such as --lookback-minutes and --timeframe.
  - Enables or disables scanner.submit with conservative paper limits via set-submit.
  - Supports dry-run output for moving-average/confirmed-trend seeding, scanner patch previews, and submit-control previews.
- scripts/seed_paper_trade_universe.py
  - Seeds a broader liquid paper-trading universe.
  - Default symbols: SPY, QQQ, NVDA, TSLA, IWM, AMZN, MSFT, GOOGL, META, AAPL.
  - Creates/updates moving-average call/put and confirmed-trend call/put strategies for each symbol.
  - Enables scanner.submit with one contract per order, configurable notional/spread caps, no per-symbol cap, and paper-trade-day windows.
  - Supports --dry-run, repeated --symbol, and repeated --sample-price SYMBOL=PRICE.
- scripts/analyze_paper_learning.py
  - Calls GET /api/v1/automation/learning-report and prints a structured summary.
  - Shows totals, performance, signals by strategy, intents by strategy, non-trade reasons, and job failures.
- scripts/market_day_dashboard.py
  - Prints a multi-section market-day status dashboard from automation status, positions, trade lifecycle, performance, and learning report endpoints.
- scripts/replay_paper_decisions.py
  - Replays historical paper decision context from job_runs, signals, and order intents to audit scanner behavior.
- scripts/run_exit_cycle_smoke.py
  - Runs an isolated exit-evaluation market-cycle pass to smoke-test exit logic.
- scripts/run_market_cycle_stress.py
  - Runs multiple market-cycle passes in sequence to stress-test cycle stability.
- scripts/run_paper_trade_day_cycle.ps1
  - PowerShell script to run a full market-day paper trading cycle from local environment.
- scripts/smoke_preflight.py
  - Checks sanitized settings, database connectivity/schema, Alpaca market-data reads, Alpaca trading reads, and broker reconciliation.
- scripts/run_market_cycle_smoke.py
  - Runs one market-cycle pass against the configured database and Alpaca clients.
- scripts/run_paper_submit_smoke.py
  - Runs a one-contract Alpaca paper submit smoke test.
  - Creates a smoke strategy/signal/order intent, submits to Alpaca paper, requests cancel by default, and reconciles.
  - Supports --skip-cancel and --skip-reconcile.
- scripts/run_full_smoke_suite.py
  - Runs preflight, strategy seeding, market-cycle smoke, paper submit/cancel, and deterministic unit tests.
  - Supports --skip-paper-submit, --skip-seed, and --skip-market-cycle.
- app/api/routes/automation.py
  - GET /api/v1/automation/status, bearer-protected.
  - Summarizes automation switches, active strategy scanner/submit config, and latest job runs.
  - GET /api/v1/automation/positions, bearer-protected.
  - Read-only position management status with ownership, exit config state, active exit order, P/L fields, and recommended action.
  - GET /api/v1/automation/performance, bearer-protected.
  - Read-only paper performance review with FIFO matched option round trips, realized P/L, strategy summaries, symbol summaries, recent round trips, and open lots.
  - GET /api/v1/automation/trade-lifecycle, bearer-protected.
  - Read-only open-position lifecycle view with ownership, linked entry intent, broker orders, entry fills, fill summary, active exit order, and exit recommendation.
  - GET /api/v1/automation/trade-cases, bearer-protected.
  - Read-only FIFO trade-case view for closed round trips and open lots, intended as the stable input shape for later AI review.
  - GET /api/v1/automation/learning-report, bearer-protected.
  - Aggregated diagnostic report: cycle totals, performance summary, signals by strategy, intents by strategy, non-trade reasons, and job failures. Best first endpoint for assessing system health.
- app/api/routes/health.py
  - GET /api/v1/health, public.
  - GET /api/v1/ready, bearer-protected; checks DB connection and required tables.
- app/api/routes/order_intents.py
  - POST /api/v1/order-intents/preview, bearer-protected.
  - POST /api/v1/order-intents, bearer-protected.
  - GET /api/v1/order-intents, bearer-protected.
  - POST /api/v1/order-intents/{id}/submit, bearer-protected.
  - Preview endpoint turns a signal plus an explicit option_symbol or contract_selection constraints into a durable previewed order_intent using Alpaca latest option quote data.
- app/api/routes/options.py
  - POST /api/v1/options/select-contract, bearer-protected.
  - Selects an Alpaca option contract from simple constraints and returns quote context.
- app/api/routes/strategies.py
  - POST /api/v1/strategies, bearer-protected.
  - GET /api/v1/strategies, bearer-protected.
  - GET /api/v1/strategies/{id}, bearer-protected.
  - PATCH /api/v1/strategies/{id}, bearer-protected.
  - Keeps strategy config as JSONB and supports toggling is_active.
- app/api/routes/signals.py
  - POST /api/v1/signals, bearer-protected.
  - GET /api/v1/signals, bearer-protected.
  - GET /api/v1/signals/{id}, bearer-protected.
  - PATCH /api/v1/signals/{id}, bearer-protected.
  - Captures strategy-generated or manual trade signals before order intents.
- app/api/routes/jobs.py
  - POST /api/v1/jobs/reconcile-broker, bearer-protected.
  - Manually reconciles Alpaca broker state into local durable tables.
  - POST /api/v1/jobs/scan-signals, bearer-protected.
  - Manually scans active strategy configs and creates signals only.
  - POST /api/v1/jobs/evaluate-exits, bearer-protected.
  - Evaluates latest reconciled positions against strategy scanner.exit rules and creates previewed sell order intents.
  - POST /api/v1/jobs/preview-unmanaged-exits, bearer-protected.
  - Creates previewed sell order intents for unmanaged long positions without submitting orders.
  - POST /api/v1/jobs/check-news, bearer-protected.
  - Checks configured market RSS feeds plus owned-ticker RSS searches and records results in job_runs.
  - POST /api/v1/jobs/market-cycle, bearer-protected.
  - Runs enabled market-cycle steps behind env switches.
  - POST /api/v1/jobs/market-cycle-exits, bearer-protected.
  - Runs an isolated exit-only market-cycle pass (reconcile + exit evaluation + exit submit).
  - POST /api/v1/jobs/market-cycle-stress, bearer-protected.
  - Runs multiple market-cycle passes in sequence for stress testing; does not submit orders.
  - POST /api/v1/jobs/market-maintenance, bearer-protected.
  - Runs full daily maintenance: pre-market tasks, snapshot, post-market cleanup, audit.
  - POST /api/v1/jobs/pre-market-maintenance, bearer-protected.
  - Runs pre-market maintenance tasks only.
  - POST /api/v1/jobs/post-market-maintenance, bearer-protected.
  - Runs post-market maintenance tasks only.
  - POST /api/v1/jobs/reset-trading-data, bearer-protected.
  - Resets paper trading data for a clean paper day; does not delete strategies.
- app/schemas/order_intents.py
  - Pydantic request/response schemas for order intents and broker order submission.
  - Includes schema for generated order intent previews from signals.
  - Preview schema requires exactly one of option_symbol or contract_selection.
  - Enforces limit_price rules and currently only supports time_in_force="day".
- app/schemas/strategies.py
  - Pydantic request/response schemas for strategy create, update, and read.
- app/schemas/signals.py
  - Pydantic request/response schemas for signal create, update, and read.
  - Validates confidence between 0 and 1 when supplied.
- app/schemas/jobs.py
  - Pydantic response schemas for job runs, broker reconciliation, signal scans, and market cycles.
- app/schemas/options.py
  - Pydantic request/response schemas for option contract selection.
- app/schemas/automation.py
  - Pydantic response schemas for automation status, position management, performance, trade lifecycle, trade cases, and learning report.
  - LearningReportRead: generated_at, totals, performance, signals_by_strategy, intents_by_strategy, non_trade_reasons, job_failures.
- app/services/automation_status.py
  - Service logic for reading active strategy automation config and latest job_run summaries.
  - Includes paper_trading_readiness in operational_summary so tomorrow's auto-paper switches can be checked before enabling submit.
- app/services/trade_lifecycle.py
  - Derived accounting views for open trade lifecycle and paper trade cases.
  - Joins latest position snapshots to position ownership, linked entry order intent, broker orders, fills, active exit order, and exit recommendation.
  - Reuses FIFO paper performance review for closed round trips and open lots.
- app/services/learning_report.py
  - Aggregates system health from recent job_runs, signals, order_intents, and fills.
  - Returns LearningReportResult with totals, performance, signals_by_strategy, intents_by_strategy, non_trade_reasons, and job_failures.
  - Best first diagnostic for understanding whether the system is scanning, previewing, and exiting correctly.
- app/services/market_maintenance.py
  - Pre-market and post-market daily maintenance tasks.
  - Pre-market: checks strategy configs, confirms active strategies exist, optionally seeds missing strategies.
  - Post-market: cleans stale previewed/unsubmitted order intents, records maintenance job_runs.
- app/services/trading_reset.py
  - Resets paper trading state for a new paper day.
  - Deletes open previewed order intents, clears open signals, clears position snapshots.
  - Does not delete strategies, fills, or broker_orders (historical record preserved).
  - Records reset job_runs and audit_logs.
- app/services/order_intents.py
  - Domain/service logic to submit a previewed order intent through Alpaca.
  - Domain/service logic to generate a previewed order intent from a signal and latest option quote.
  - Can select an option contract before previewing when contract_selection is supplied.
  - Creates broker_orders and updates order_intents.
  - Marks order_intents rejected when Alpaca rejects.
- app/services/strategy_templates.py
  - Builds JSON-safe preview-first paper strategy payloads.
  - Current templates include SPY upside call preview, QQQ downside put preview, SPY momentum call preview, SPY moving-average call preview, and SPY confirmed-trend call preview.
  - Templates cap estimated option notional/spread and leave auto-submit disabled.
  - Moving-average template default trigger is now bullish_cross (fires only at crossover event, not sustained trend).
- app/services/option_contracts.py
  - Domain/service logic to select an active tradable Alpaca option contract.
  - First-pass selection chooses earliest expiration and strike closest to target_strike or underlying_price.
  - Fetches latest option quote context for the selected contract.
- app/services/broker_reconciliation.py
  - Domain/service logic to fetch recent Alpaca orders, fill activities, and current positions.
  - Upserts broker_orders, inserts new fills, snapshots positions, and records job_runs.
- app/services/signal_scanner.py
  - Domain/service logic for manual signal scans.
  - Reads active strategies with config.scan_signals entries.
  - Supports config.scanner.type="price_threshold" using latest Alpaca stock quote midpoint.
  - Supports config.scanner.type="percent_change" using recent Alpaca stock bars.
  - Supports config.scanner.type="moving_average" using recent Alpaca stock bars, short/long windows, and bullish/bearish cross or trend triggers.
  - Supports config.scanner.type="trend_confirmation" using moving-average alignment plus a minimum lookback percent change to reduce choppy false positives.
  - Supports scanner dedupe_minutes to suppress repeated equivalent signals.
  - Creates signals; market_cycle can optionally preview/submit scanner-created signals behind separate switches.
  - Records job_runs and audit_logs for success/failure.
- app/services/market_cycle.py
  - Orchestrates enabled market-cycle steps.
  - Currently supports scan_signals and broker reconciliation.
  - Supports optional auto-preview for scanner-created signals when MARKET_CYCLE_PREVIEW_ENABLED=true and strategy config has scanner.preview.enabled=true.
  - Supports optional exit evaluation when MARKET_CYCLE_EXIT_ENABLED=true and strategy config has scanner.exit.enabled=true.
  - Supports controlled paper auto-submit for same-cycle previewed entry order_intents when MARKET_CYCLE_SUBMIT_ENABLED=true and strategy config has scanner.submit.enabled=true.
  - Supports controlled paper auto-submit for same-cycle exit order_intents when MARKET_CYCLE_SUBMIT_ENABLED=true and strategy config has scanner.exit.submit.enabled=true.
  - Enforces submit config max_orders_per_cycle, max_contracts_per_order, optional max_contracts_per_cycle, optional max_notional_per_order, optional max_open_contracts_per_symbol, optional max_open_contracts_per_strategy, optional max_orders_per_trading_day, optional trade_windows, and allowed_sides.
  - Existing open-contract checks use broker_orders joined to the strategy order_intents.
  - Pending signal pickup filters to current trading day only (Signal.created_at >= today ET start) to prevent stale signals from prior days being previewed at Monday open.
  - Records job_runs and audit_logs for success/failure.
- app/services/audit_logs.py
  - Small helper for writing audit_logs records consistently.
- app/services/position_exits.py
  - Evaluates latest position_snapshots for exit rules.
  - Links positions back to the latest entry order_intent/strategy for the held symbol when possible.
  - Reports position_ownership with managed/unmanaged status and reasons such as no linked entry, inactive strategy, or missing exit config.
  - Supports manual preview_unmanaged_position_exits for creating sell previews on unmanaged long positions.
  - Supports get_position_management_statuses for read-only position dashboard data and recommendations.
  - Supports scanner.exit profit_target_percent, stop_loss_percent, max_days_to_expiration, and max_hold_hours.
  - max_hold_hours triggers exit after N hours since entry fill regardless of P/L; uses UTC-normalized fill time.
  - Timezone bug fixed: DTE calculations now use America/New_York date (not UTC date), so calculations remain correct after midnight UTC on Eastern trading days.
  - Creates previewed sell order_intents with quote context and audit logs.
- app/services/performance_review.py
  - Read-only paper performance review using existing fills, broker_orders, order_intents, and strategies.
  - Matches buy/sell option fills FIFO by symbol and strategy.
  - Reports realized P/L, win/loss counts, win rate, per-strategy summaries, recent round trips, and open lots.
- app/services/news_scanner.py
  - Fetches RSS news for broad market context and currently owned tickers.
  - Default broad scan covers US indexes, Fed/rates/inflation/economic data, US economy/yields/dollar/recession/GDP, world markets/geopolitical risk/oil/war/tariffs, and earnings/volatility/credit/banking headlines.
  - Reduces option contract symbols to their underlying ticker for owned-ticker searches.
  - Tags headlines with simple impact keywords such as Fed, rates, inflation, earnings, downgrade, lawsuit, merger, oil, yields, and volatility.
  - Produces news_risk_v2 risk_assessment with market_risk_level, should_block_new_entries, blocking_reasons, manual_review_symbols, per-ticker risk, review_items, and ignored_items.
  - Down-ranks stale headlines, low-quality sources, and noisy options-chain/trading-content ticker results before they can block entries.
  - Records news_scan job_runs and audit logs.
- app/integrations/alpaca.py
  - Thin httpx-based Alpaca Trading API integration.
  - Currently supports POST /v2/orders for order intent submission.
  - Supports GET /v2/orders, GET /v2/account/activities/FILL, and GET /v2/positions for reconciliation.
  - Supports GET /v2/options/contracts for option contract selection.
  - Supports GET https://data.alpaca.markets/v2/stocks/quotes/latest for stock quote scanner rules.
  - Supports GET https://data.alpaca.markets/v2/stocks/bars for percent-change scanner rules.
  - Supports GET https://data.alpaca.markets/v1beta1/options/quotes/latest for latest option quote previews.
  - Converts Alpaca responses into Pydantic models.
- tests/test_order_intent_submission.py
  - unittest coverage for order submission service behavior and schema validation.
- tests/test_broker_reconciliation.py
  - unittest coverage for broker reconciliation success/failure behavior using fake Alpaca clients.
- tests/test_option_contract_selection.py
  - unittest coverage for option contract selection and route error mapping using fake Alpaca clients.
- tests/test_signal_scanner.py
  - unittest coverage for manual scanner success, inactive strategy filtering, price-threshold and percent-change rules, malformed specs, duplicate suppression, and failure recording.
- tests/test_render_job_runner.py
  - unittest coverage for the Render cron kill switch, job URL construction, retryable HTTP errors, and non-retryable HTTP errors.
- tests/test_market_cycle.py
  - unittest coverage for market-cycle orchestration, env switches, and failure recording.
- tests/test_automation_status.py
  - unittest coverage for automation status summary shape.
- tests/test_automation_guard.py
  - unittest coverage for automation guard decisions: global kill switch, paper mode requirement, daily cap, open position cap.
- tests/test_performance_review.py
  - unittest coverage for FIFO P/L matching, win/loss accounting, and open lot reporting.
- tests/test_position_exits.py
  - unittest coverage for position exit rule evaluation, max_hold_hours trigger, and timezone-correct DTE calculation.
- tests/test_news_scanner.py
  - unittest coverage for RSS headline parsing, freshness filtering, and risk assessment logic.
- tests/test_signals.py
  - unittest coverage for signal create/update routes, audit log writes, and strategy validation.
- tests/test_strategies.py
  - unittest coverage for strategy create/update routes, is_active toggle, and config JSONB handling.
- tests/test_strategy_templates.py
  - unittest coverage for strategy template payload shapes and config keys.
- tests/test_strategy_tuning_script.py
  - unittest coverage for tune_paper_strategies.py CLI command behavior.
- tests/test_market_maintenance.py
  - unittest coverage for pre-market and post-market maintenance task behavior.
- tests/test_trade_lifecycle.py
  - unittest coverage for trade lifecycle and trade case service behavior.
- tests/test_trading_reset.py
  - unittest coverage for trading reset service: correct tables cleared, strategies preserved.
- tests/test_routes.py
  - unittest/FastAPI TestClient coverage for public/protected routes, readiness, create happy paths, reconciliation route mapping, option contract selection route mapping, signal scan route mapping, and market-cycle route mapping.

Current database tables implemented:
- strategies
- signals
- order_intents
- broker_orders
- fills
- position_snapshots
- job_runs
- audit_logs
- trade_cases (schema ready; service implementation TBD)
- ai_trade_reviews (schema ready; service implementation TBD)
- strategy_change_suggestions (schema ready; service implementation TBD)

Current behavior implemented:
- Root endpoint.
- Public health endpoints.
- Protected automation status endpoint for market-cycle switches, active strategy automation config, and latest job runs.
- Auth-protected readiness endpoint that checks DB connectivity and required tables.
- Single admin bearer-token auth.
- Environment-driven settings.
- Render-compatible Postgres URL handling.
- Auto migrations at startup for production/staging or AUTO_MIGRATE_ON_STARTUP=true.
- Initial trading database schema.
- Create previewed order intent.
- List order intents, optionally filtered by status.
- Create/list/read/update strategies.
- List strategies can filter by is_active.
- Strategy create/update writes audit_logs.
- Create/list/read/update signals.
- List signals can filter by strategy_id, status, and symbol.
- Signal create/update validates provided strategy_id exists.
- Signal create/update writes audit_logs.
- Generate previewed order intents from signals using Alpaca latest option quotes.
- Generated previews can either use an explicit option_symbol or select a contract from contract_selection constraints.
- Generated previews derive a limit price from midpoint when no limit_price is supplied.
- Generated previews store bid/ask/midpoint/spread/estimated notional context and selection context in order_intents.preview.
- Generated previews default to the global MAX_ESTIMATED_PREMIUM_PER_ORDER notional guard, with optional max_estimated_notional and max_spread overrides/constraints.
- Select an option contract by underlying, call/put, expiration filters, and optional target strike/underlying price.
- Option contract selection can skip quoted contracts that exceed optional max_estimated_notional or max_spread constraints.
- Option contract selection returns selected contract details and latest quote context without creating an order.
- Manual order_intent creation validates provided strategy_id and signal_id exist.
- Manual order_intent creation rejects strategy_id/signal_id combinations that do not match.
- Submit previewed order intent to Alpaca paper/live base URL depending on ALPACA_PAPER.
- Store returned Alpaca order as broker_orders.
- Update order_intent status/submitted_at after submission.
- Mark order_intent rejected and save rejection reason if Alpaca rejects.
- Request broker order cancellation through POST /api/v1/order-intents/{id}/cancel.
- Cancel requests mark the local order_intent and broker_order as cancel_requested; reconciliation remains responsible for final broker status.
- Manually reconcile broker state from Alpaca through POST /api/v1/jobs/reconcile-broker.
- Render cron blueprint exists for market-cycle every 30 minutes, disabled by default via SCHEDULED_JOBS_ENABLED=false.
- Actual Render cron service has been manually created/enabled and has returned successful market-cycle 200 responses.
- Manually scan active strategy configs through POST /api/v1/jobs/scan-signals.
- Signal scanning creates signal records; market-cycle handles optional preview/submit automation.
- Signal scanning supports seeded config.scan_signals entries and live config.scanner price_threshold/percent_change rules.
- Signal scanning suppresses duplicate recent scanner signals by default using dedupe_minutes.
- Market-cycle endpoint can run scan and reconciliation in one protected call.
- Market-cycle endpoint can auto-create previewed order intents for scanner-created signals when preview switches/config are enabled.
- Market-cycle endpoint can evaluate current positions for exit previews when MARKET_CYCLE_EXIT_ENABLED=true.
- Market-cycle endpoint can check market and owned-ticker news when MARKET_CYCLE_NEWS_ENABLED=true.
- High market-level news risk blocks new entry previews for that market-cycle run and flags affected owned tickers for manual review; it does not auto-sell from headlines.
- Market-cycle endpoint can auto-submit only same-cycle previewed order intents when submit switches/config are enabled.
- Auto-submit enforces strategy-level sizing and existing-exposure controls before broker submission.
- Auto-submit enforces optional daily order caps and configured trading windows before broker submission.
- Reconciliation upserts recent orders into broker_orders.
- Reconciliation mirrors linked broker order status/submitted_at back to order_intents when it can identify the intent.
- Reconciliation inserts new fill activities into fills.
- Reconciliation snapshots current positions into position_snapshots.
- Reconciliation records success/failure metadata in job_runs.
- Pending signal pickup in market_cycle is capped to the current trading day (ET); stale signals from prior days are not previewed.
- Daily maintenance jobs: pre-market checks, post-market cleanup of stale order intents, job_run recording.
- Trading reset job: clears open signals, open previewed intents, and position snapshots for a clean paper day.
- Exit evaluation supports max_hold_hours: exits a position after N hours since entry fill, regardless of P/L.
- Exit evaluation uses America/New_York date for DTE calculations (not UTC), preventing off-by-one-day errors after midnight UTC.
- Learning report aggregates diagnostic data across recent job_runs, signals, intents, and fills into one endpoint response.
- Audit logs are written for:
  - order_intent.created
  - order_intent.submitted
  - order_intent.rejected
  - order_intent.cancel_requested
  - order_intent.exit_previewed
  - news_scan.succeeded
  - news_scan.failed
  - strategy.created
  - strategy.updated
  - signal.created
  - signal.updated
  - order_intent.previewed
  - broker_reconciliation.succeeded
  - broker_reconciliation.failed
  - signal_scan.succeeded
  - signal_scan.failed
  - market_cycle.succeeded
  - market_cycle.failed
  - trading_reset.succeeded
  - trading_reset.failed
  - market_maintenance.succeeded
  - market_maintenance.failed
- Automation Safety Gates were added for market-cycle auto-submit.
  - New env/settings: TRADING_AUTOMATION_ENABLED=false, AUTO_SUBMIT_REQUIRES_PAPER=true, MAX_AUTO_ORDERS_PER_CYCLE=1, MAX_AUTO_ORDERS_PER_DAY=3, MAX_OPEN_POSITIONS=3, MAX_OPEN_POSITIONS_PER_SYMBOL=1, MAX_CONTRACTS_PER_ORDER=1, MAX_ESTIMATED_PREMIUM_PER_ORDER=250.
  - New env/settings: MARKET_CYCLE_EXIT_ENABLED=false controls market-cycle position exit evaluation.
  - New env/settings: MARKET_CYCLE_NEWS_ENABLED=false controls market-cycle news checks.
  - Guard service: app/services/automation_guard.py with can_auto_submit_order_intent(db, order_intent, cycle_id=None).
  - Market-cycle auto-submit now checks the guard before calling submit_order_intent; blocked intents are skipped, included in submit errors, and audited as order_intent.auto_submit_skipped.
- GET /api/v1/automation/status now includes the global safety fields and paper mode.
  - operational_summary now includes paper_trading_readiness with blockers, warnings, submit-ready strategies, and preview-only strategies.
  - GET /api/v1/automation/performance reports paper P/L from matched fills for strategy tuning.
  - Postman automation status and market-cycle coverage includes safety-field and skip-visibility assertions.
  - Tests added/updated for guard decisions, market-cycle guard skips, and automation status response shape.
- Scanner no-op observability was added.
  - scan_signals responses and job_runs.details now include no_signal_reasons.
  - Market-cycle scan details carry through no_signal_reasons.
  - Reasons explain successful scans with no signals, such as no recent bars, no usable quotes, or thresholds not crossed.
  - This is observability only; scanner strategy thresholds and signal-generation behavior were not changed.
- Basic tests for order submission success/rejection and order intent schema rules.
- Basic tests for reconciliation success/failure service behavior.
- Basic tests for option contract selection and selector-backed preview generation.
- Basic tests for manual signal scanning.
- Tests verify audit log writes for order intent, strategy, signal, and reconciliation workflows.
- Route-level tests verify public health routes, readiness success/failure, protected route auth, automation status route mapping, reference-validation responses, happy-path creates, broker reconciliation route error mapping, option contract selection route mapping, and signal scan route mapping.
- Postman collection for smoke testing deployed/local API.
  - Includes health/ready, automation status, reconcile, market-cycle, scan-signals, strategy/signal/order-intent flows, and option contract selection.

Current important limitations/gaps:
- Scanner/signal-generation logic supports quote-threshold, percent-change, moving-average, and confirmed-trend rules, but no broader indicator scoring/backtesting yet.
- Option contract selection is first-pass only; no delta/Greeks/moneyness scoring yet.
- Exit evaluation supports simple P/L, expiration, and max_hold_hours rules; no trailing stops or AI-assisted exits yet.
- News scanning is RSS/headline risk gating with lightweight freshness/source-quality filters; no persistent article table, source ranking model, sentiment model, article-body reading, or auto-sell decisioning yet.
- Manual order_intent creation still accepts client-provided preview data.
- No formal status enum/state machine yet; statuses are plain strings.
- No idempotency/retry protection beyond using order_intent id as Alpaca client_order_id.
- Broker reconciliation runs as part of the market-cycle endpoint when MARKET_CYCLE_RECONCILE_ENABLED=true.
- Reconciliation fetch limits are simple query params; no pagination loop yet.
- Scheduled market-cycle is scaffolded in render.yaml as disabled by default, but the actual Render cron has been manually enabled for testing.
- Audit logging exists for core manual broker-touching workflows, but not for every future object type yet.
- trade_cases, ai_trade_reviews, and strategy_change_suggestions tables exist in the schema (migration 0003) and ORM models, but the service that populates them from closed trades is not yet implemented.
- No local Docker Compose/Postgres helper yet.
- No integration tests against a real database yet.

Planned scheduled jobs:
1. Market-hours job every 5 minutes:
   - scan for signals
   - follow up on orders
   - reconcile broker state

2. Daily maintenance job:
   - pre-market tasks
   - end-of-day tasks
   - snapshot positions
   - eventually prepare closed trade cases for AI review

AI review layer (schema ready, service TBD):
- trade_cases, ai_trade_reviews, strategy_change_suggestions tables exist from migration 0003.
- ORM models exist in app/db/models.py.
- After trades close, a service will store enough context for offline AI review so AI can suggest strategy improvements.
- AI rules:
  - AI can review completed trades and suggest changes.
  - AI must not auto-change live trading logic.
  - Eventually capture rejected trades/signals for comparison analysis.

Recommended next concrete implementation step:
- Use the automation status endpoint against Render to confirm production has an active strategy config.
- Then add or update one active paper strategy if status shows active_strategies=[].
- After that, enable MARKET_CYCLE_PREVIEW_ENABLED=true while keeping MARKET_CYCLE_SUBMIT_ENABLED=false.

Likely next steps after deployed preview verification:
1. Implement the trade_cases population service so closed round trips flow into the AI review schema.
2. Build AI review service that sends trade_cases context to Claude and stores ai_trade_reviews.
3. Improve broker-order/position linkage for more exact open position accounting.

Important product rules:
- Single user only.
- Keep security simple but real.
- Do not overengineer auth or multi-tenant logic.
- Paper trading is the current target.
- Live trading comes later, so structure code with that future in mind.
- Prefer clear modules, explicit schemas, and durable database records over clever shortcuts.
- Source of truth for broker order/fill/position state should remain Alpaca.
- Local DB should be durable state/cache/history, not an invented broker truth.

How I want Codex to work:
- First inspect the repo and infer the current project state from existing code.
- Prefer working with the code that already exists rather than reinventing the project.
- Anchor suggestions to the current repo structure.
- Be explicit about what is already implemented, what is stubbed, and what is missing.
- Prefer small, safe, reviewable steps.
- Keep Windows local development in mind.
- Keep Bash-friendly local commands in mind because I may run things from Git Bash on Windows.
- Avoid unnecessary abstractions.
- Do not introduce services or infra I do not need yet.
- Keep the app easy to run locally.
- If asked to make code changes, make them minimally invasive and consistent with current architecture.
- When editing code, explain why each change matters.
- Prefer practical progress over theoretical redesigns.

Coding preferences:
- Clear Python.
- Typed where reasonable.
- Pydantic schemas where appropriate.
- SQLAlchemy/Alembic style patterns already used in the repo.
- Environment-variable driven configuration.
- Predictable API routes.
- Clean separation between API routes, broker integration, persistence, and domain logic.
- Avoid clever abstractions until repetition/risk justifies them.

Local dev expectations:
- The project should run locally on Windows.
- Commands should be easy to copy/paste.
- Support both PowerShell and Git Bash workflows.
- Startup should remain scriptable with simple local run scripts.

Useful local commands:
- PowerShell setup:
  python -m venv .venv
  .\.venv\Scripts\Activate.ps1
  python -m pip install --upgrade pip
  pip install -r requirements.txt
  Copy-Item env.example .env
  .\run-local.ps1

- Git Bash setup:
  python -m venv .venv
  source .venv/Scripts/activate
  python -m pip install --upgrade pip
  pip install -r requirements.txt
  cp env.example .env
  ./run-local.sh

- Run migrations:
  python -m alembic upgrade head

- Run current tests:
  .\.venv\Scripts\python.exe -m unittest discover -s tests

- Run deterministic tests when .env has live/E2E values:
  $env:ADMIN_API_TOKEN='change-me'; $env:MARKET_CYCLE_PREVIEW_ENABLED='false'; $env:MARKET_CYCLE_SUBMIT_ENABLED='false'; $env:TRADING_AUTOMATION_ENABLED='false'; $env:MAX_AUTO_ORDERS_PER_DAY='3'; .\.venv\Scripts\python.exe -m unittest discover -s tests

- Run smoke tests:
  .\.venv\Scripts\python.exe .\scripts\smoke_preflight.py
  .\.venv\Scripts\python.exe .\scripts\run_market_cycle_smoke.py
  .\.venv\Scripts\python.exe .\scripts\run_paper_submit_smoke.py
  .\.venv\Scripts\python.exe .\scripts\run_full_smoke_suite.py

- Manual broker reconciliation:
  curl -X POST "http://127.0.0.1:8000/api/v1/jobs/reconcile-broker?order_limit=100&fill_page_size=100" -H "Authorization: Bearer change-me"

- Deployed smoke checks:
  curl.exe --ssl-no-revoke -L --max-time 90 https://stocks-api-z11i.onrender.com/
  curl.exe --ssl-no-revoke -L --max-time 90 https://stocks-api-z11i.onrender.com/health
  curl.exe --ssl-no-revoke -L --max-time 90 https://stocks-api-z11i.onrender.com/api/v1/health

When Codex responds after reading this:
- Start by telling me what the repo currently does.
- Then list the most important gaps.
- Then recommend the next concrete coding step.
- If generating code, make it directly usable in this repo.
