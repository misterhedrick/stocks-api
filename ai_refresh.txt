You are helping me build a single-user Python stock/options paper trading API.

=== CURRENT SESSION STARTUP RULE ===

Every time this file is read, create or switch to a feature branch off of develop:

    git fetch origin develop
    git checkout develop && git pull origin develop
    git checkout -b <feature-branch-name>

Git workflow rules (same as README):
- All work is done on a feature branch. Feature branches are merged to develop when ready.
- Never commit directly to develop or master.
- All merges from develop -> master require explicit human approval.
- Claude Code must prompt for confirmation before any push or merge targeting develop or master.

=== PROJECT IDENTITY ===

Project name:
stocks-api

Repository:
https://github.com/misterhedrick/stocks-api.git

Hosted Render URL:
https://stocks-api-z11i.onrender.com/

Local workspace:
c:\Users\Miste\OneDrive\Documents\dev\stocks-api

Branch strategy:
- master: Render deploys from this branch. Merges here require explicit human approval.
- develop: integration branch. Feature branches merge here. Never commit directly.
- feature branches: all work happens here first, then merged to develop.
- backup: failsafe stable snapshot when needed.

Current mode:
- Paper-trading first.
- Live trading later.
- Single user.
- FastAPI + Postgres + SQLAlchemy/Alembic + Alpaca + Render.
- Single admin bearer token auth.

=== HIGH-LEVEL ARCHITECTURE ===

Core flow:

strategy config
-> scanner creates signal
-> preview creates order_intent
-> optional auto-submit sends Alpaca paper order
-> broker reconciliation imports orders/fills/positions
-> exit cycle evaluates positions
-> post-market maintenance populates trade_cases
-> future AI review reads trade_cases and proposes strategy improvements

Execution rule:
- All trade execution must flow through previewed order_intents.
- Alpaca is source of truth for broker orders, fills, and positions.
- AI can recommend strategy changes only; it must not auto-change live logic.

=== STRATEGY REFINEMENT CONSOLIDATION STATE ===

Implemented on develop:
- /api/v1/automation/learning-report includes refinement_candidates grouped by scanner_type, not symbol-specific strategy rows.
- Post-market maintenance saves the generated learning report inside review_snapshots.raw_payload.learning_report.
- REVIEW_SNAPSHOT_RETENTION_DAYS controls review_snapshots retention; default is 45 days.
- Post-market maintenance prunes old review_snapshots after creating the latest snapshot and AI reviews.
- /api/v1/automation/strategy-refinement reads recent saved snapshots and builds a tuning queue with:
  - priority score trends
  - minimum evidence gates
  - readiness statuses
  - signal vs option-selection vs exit/risk focus
  - before/after windows around recorded tuning decisions
- strategy_tuning_decisions persists human-approved tuning decisions, evidence snapshot IDs, expected effect, proposed config patches, and later outcome summaries.
- /api/v1/automation/strategy-tuning-decisions supports list/create/update for tuning decisions.

Important rule:
- strategy_tuning_decisions and strategy_change_suggestions are human-review records only. They must not automatically apply config or strategy logic changes.

=== CURRENT DEPLOYED / OPERATIONAL STATE ===

Render web service:
- service: stocks-api
- branch: master
- plan: free
- healthCheckPath: /health
- startCommand: bash ./start.sh
- expected secrets: DATABASE_URL, ADMIN_API_TOKEN, ALPACA_API_KEY, ALPACA_API_SECRET
- APP_ENV=production
- AUTO_MIGRATE_ON_STARTUP=true
- ALPACA_PAPER=true

Render cron services:

1. Symbol-specific entry crons
   - Entry cycle: scan/reconcile/news/preview/submit for one symbol per job.
   - Endpoints:
     /api/v1/jobs/market-entry-cycle?symbol=SPY&scan_limit=100&order_limit=100&fill_page_size=100
     /api/v1/jobs/market-entry-cycle?symbol=QQQ&scan_limit=100&order_limit=100&fill_page_size=100
     /api/v1/jobs/market-entry-cycle?symbol=AAPL&scan_limit=100&order_limit=100&fill_page_size=100
     /api/v1/jobs/market-entry-cycle?symbol=MSFT&scan_limit=100&order_limit=100&fill_page_size=100
     /api/v1/jobs/market-entry-cycle?symbol=NVDA&scan_limit=100&order_limit=100&fill_page_size=100
   - Current crons:
     SPY: 0-55/5 14-19 * * 1-5
     QQQ: 1-55/5 14-19 * * 1-5
     AAPL: 2-55/5 14-19 * * 1-5
     MSFT: 3-55/5 14-19 * * 1-5
     NVDA: 4-55/5 14-19 * * 1-5
   - During EDT these run from 10:00am through 3:55pm Eastern, staggered by symbol, with no entry cron after 3:55pm.
   - Render cron is UTC-only and not DST-aware. When Eastern switches to EST, change the hour window from 14-19 UTC to 15-20 UTC.

2. stocks-api-market-exits
   - Exit-only cycle: reconcile + exit evaluation + optional exit submit.
   - Endpoint:
     /api/v1/jobs/market-cycle-exits?limit=100&order_limit=100&fill_page_size=100&phase_timeout_seconds=45
   - Current cron:
     */1 13-19 * * 1-5
   - During EDT this runs every minute from about 9:00am through 3:59pm Eastern.
   - This remains separate from entry; entries and exits stop before the 4:00pm market close.
   - Render cron is UTC-only and not DST-aware. When Eastern switches to EST, change the hour window from 13-19 UTC to 14-20 UTC.

3. stocks-api-market-maintenance
   - Pre/post-market maintenance.
   - Endpoint:
     /api/v1/jobs/market-maintenance?phase=auto&news_enabled=false
   - Current cron:
     30 12,21 * * 1-5
   - Post-market maintenance also populates trade_cases.

Emergency stops:
- TRADING_AUTOMATION_ENABLED=false stops all auto-submit.
- MARKET_CYCLE_SUBMIT_ENABLED=false stops submit while keeping scans/reconcile.
- SCHEDULED_JOBS_ENABLED=false stops cron runner execution.
- MARKET_CYCLE_EXIT_ENABLED=false pauses exit evaluation.

Paper-trading safeguards:
- ALPACA_PAPER=true
- AUTO_SUBMIT_REQUIRES_PAPER=true

Automation risk caps:
- MAX_AUTO_ORDERS_PER_CYCLE caps submitted orders per cycle.
- MAX_AUTO_ORDERS_PER_DAY caps submitted orders per day.
- MAX_OPEN_POSITIONS caps total open positions.
- MAX_OPEN_POSITIONS_PER_SYMBOL caps open positions per underlying.
- MAX_CONTRACTS_PER_ORDER caps order quantity.
- MAX_ESTIMATED_PREMIUM_PER_ORDER caps estimated premium per order.

=== CURRENT DATABASE / STRATEGY STATE ===

Current active strategy type / preview profile names should be evaluator-backed only after migrations and reseeding:
- moving_average
- momentum_rate_of_change
- rsi_reversal
- macd_crossover
- mean_reversion
- breakout_price_threshold
- volume_confirmed_breakout
- volatility_squeeze
- support_resistance
- vwap_reclaim
- opening_range_breakout
- relative_strength
- time_series_momentum
- market_regime_filter
- pairs_relative_value
- options_spread_candidate

Legacy scanner types are deactivated and unsupported:
- price_threshold
- percent_change
- trend_confirmation

Important migration note:
- `alembic heads` should report a single head: 0011_strategy_tuning_decisions.

Preview profile env pattern:
- PREVIEW_PROFILE_<PROFILE>_<SETTING>

Examples:
- PREVIEW_PROFILE_MOVING_AVERAGE_MIN_OPEN_INTEREST=50
- PREVIEW_PROFILE_MOVING_AVERAGE_MAX_ESTIMATED_NOTIONAL=5000
- PREVIEW_PROFILE_MOVING_AVERAGE_MAX_SPREAD_PERCENT=35
- PREVIEW_PROFILE_MOMENTUM_RATE_OF_CHANGE_MIN_OPEN_INTEREST=50
- PREVIEW_PROFILE_MOMENTUM_RATE_OF_CHANGE_MAX_ESTIMATED_NOTIONAL=5000
- PREVIEW_PROFILE_RSI_REVERSAL_MAX_ESTIMATED_NOTIONAL=5000
- PREVIEW_PROFILE_VOLUME_CONFIRMED_BREAKOUT_MAX_ESTIMATED_NOTIONAL=5000

The runtime contract selector can use profile-specific env limits when preview_profile is supplied.

=== CURRENT SYMBOL UNIVERSE ===

scripts/seed_trade_universe.py default universe:

SPY, QQQ, NVDA, AAPL, MSFT

The GitHub Actions seed workflow defaults to the same five-symbol universe and accepts a comma-separated symbol override.

The script now seeds one active global strategy per scanner type. Each strategy scans the full configured symbol universe, and signal direction determines call/put preview side:
- moving_average
- momentum_rate_of_change
- rsi_reversal
- macd_crossover
- mean_reversion
- breakout_price_threshold
- volume_confirmed_breakout
- volatility_squeeze
- support_resistance
- vwap_reclaim
- opening_range_breakout
- relative_strength
- time_series_momentum
- market_regime_filter
- pairs_relative_value
- options_spread_candidate

Default seeded set:
- 16 scanner-type strategies.

Reseeding deactivates legacy symbol-specific preview strategies such as `MSFT momentum rate-of-change put preview` and replaces them with global scanner-type strategies such as `momentum_rate_of_change`.

Current paper data-gathering profile:
- strictness_level=0.70
- strictness_profile=selective_winner_bias
- max_estimated_notional=5000
- max_notional_per_order=5000
- min_open_interest=50
- max_spread=0.35
- max_spread_percent=35
- seeded submit caps remain high enough to allow good signals through:
  - max_orders_per_cycle=100
  - max_orders_per_trading_day=500
  - max_open_contracts_per_strategy=100

The profile is intentionally semi-picky at the signal level: it favors cleaner setups and fewer duplicate entries, but it does not throttle good signals merely to reduce count. Runtime env caps still provide global paper-safety limits.

Current Render paper exposure caps:
- MAX_AUTO_ORDERS_PER_DAY=60
- MAX_OPEN_POSITIONS=30

Current Render preview profile notional caps by strategy type:
- moving_average=5000
- momentum_rate_of_change=5000
- rsi_reversal=5000
- macd_crossover=5000
- mean_reversion=5000
- breakout_price_threshold=5000
- volume_confirmed_breakout=5000
- volatility_squeeze=5000
- support_resistance=5000
- vwap_reclaim=5000
- opening_range_breakout=5000
- relative_strength=5000
- time_series_momentum=5000
- market_regime_filter=5000
- pairs_relative_value=5000
- options_spread_candidate=5000

The market_regime_filter, pairs_relative_value, and options_spread_candidate scanners are signal-only. They can create review/context signals, but the market-cycle preview gate marks them signal_only and does not create option previews or order intents. Paired, spread, and other multi-leg orders require separate order plumbing before these signals can submit true strategy-specific trades.

The 2026-05-18 tuning pass keeps SPY enabled and tunes scanner/profile behavior by strategy type. Do not pause the SPY entry cron unless a later human-approved review explicitly changes that decision.

Apply the 2026-05-18 strategy-type tuning batch with:

    python scripts/tune_strategies.py apply-2026-05-18-strategy-type-batch --dry-run
    python scripts/tune_strategies.py apply-2026-05-18-strategy-type-batch

The batch patches:
- support_resistance: stricter level quality and distance.
- momentum_rate_of_change: longer lookback, higher ROC threshold, lower max extension, wider controlled stop.
- moving_average: stricter change, separation, distance, and dedupe.
- mean_reversion: stricter Bollinger setup, tighter distance-to-middle, wider controlled stop.
- rsi_reversal: stricter 30/70 levels with trend-conflict rejection.
- volume_confirmed_breakout: higher volume confirmation and tighter breakout distance.

The 2026-06-11 fresh-paper tuning batch uses the 2026-06-09 through 2026-06-10 paper evidence to tighten only the highest-confidence scanner knobs before a clean paper-account restart. Apply it with:

    python scripts/tune_strategies.py apply-2026-06-11-fresh-paper-tuning-batch --dry-run
    python scripts/tune_strategies.py apply-2026-06-11-fresh-paper-tuning-batch

The batch patches:
- mean_reversion: bollinger_stddev=2.50 and max_distance_to_middle_percent=0.75.
- momentum_rate_of_change: change_above_percent=0.75, change_below_percent=-0.75, max_extension_percent=1.00.
- support_resistance: max_distance_percent=0.35.
- time_series_momentum: min_trend_percent=2.00.

The 2026-06-16 support-resistance winner-bias decision keeps support_resistance active but restricts seeded support_resistance to breakout/breakdown entries after the 2026-06-15 paper snapshot showed 0 wins and 5 losses for support_resistance:
- mode=breakout
- breakout_buffer_percent=0.20
- max_distance_percent=0.35

Judge the support_resistance winner-bias outcome after 3 post-market snapshots before restoring bounce/rejection mode.

The 2026-06-17 exit-risk and breakout-quality batch uses the 2026-06-15 through 2026-06-17 paper evidence to tighten loss-heavy scanners:
- mean_reversion: scanner.exit.stop_loss_percent=8 and scanner.exit.max_hold_hours=4.
- volatility_squeeze: breakout_buffer_percent=0.20 and max_breakout_distance_percent=1.50.
- breakout_price_threshold: breakout_buffer_percent=0.20 and max_breakout_distance_percent=1.25.

Apply it with:

    python scripts/tune_strategies.py apply-2026-06-17-risk-breakout-quality-batch --dry-run
    python scripts/tune_strategies.py apply-2026-06-17-risk-breakout-quality-batch

Judge this batch after 3 post-market snapshots using the related strategy_tuning_decisions.

Current momentum rate-of-change Render preview override:
- PREVIEW_PROFILE_MOMENTUM_RATE_OF_CHANGE_MIN_OPEN_INTEREST=50
- PREVIEW_PROFILE_MOMENTUM_RATE_OF_CHANGE_MAX_ESTIMATED_NOTIONAL=5000

Current paper exit defaults:
- STRATEGY_PROFIT_TARGET_PERCENT=25
- STRATEGY_STOP_LOSS_PERCENT=10
- STRATEGY_STOP_LOSS_MIN_DOLLARS=10

The 2026-05-18 strategy-type batch overrides scanner.exit.stop_loss_percent to 15 for momentum_rate_of_change and mean_reversion only, as a controlled "give options more time" test paired with stricter entry filtering.

The stop loss fires only when both the percent loss threshold and the minimum dollar loss floor are met. Existing strategies can be patched with:

    python scripts/update_strategy_stop_loss.py --dry-run
    python scripts/update_strategy_stop_loss.py

=== RECENT IMPORTANT CODE CHANGES ON DEVELOP ===

1. Preview profile support

Files touched/added earlier:
- app/core/config.py
- app/services/preview_profiles.py
- app/schemas/options.py
- app/services/option_contracts.py
- scripts/update_strategy_preview_profiles.py
- .github/workflows/update-strategy-preview-profiles.yml

Purpose:
- Allow runtime contract selection limits by strategy/signal profile.
- Avoid patching all DB strategy rows every time fine-tuning values change.

2. Signal strategy planning docs

Folder:
- docs/signal-strategies/

Each strategy now has its own folder:
- moving-average-trend-following/
- momentum-rate-of-change/
- breakout-price-threshold/
- mean-reversion/
- rsi-overbought-oversold/
- macd-crossover/
- support-resistance/
- volume-confirmed-breakout/
- volatility-squeeze/
- shared/

3. Signal evaluation foundation and scanner routing

Implemented on develop:
- app/services/signals/__init__.py
- app/services/signals/candles.py
- app/services/signals/indicators.py
- app/services/signals/evaluators/__init__.py
- app/services/signals/evaluators/base.py
- app/services/signals/evaluators/momentum.py
- app/services/signals/evaluators/moving_average.py
- app/services/signals/evaluators/rsi.py
- app/services/signals/evaluators/macd.py
- app/services/signals/evaluators/mean_reversion.py
- app/services/signals/evaluators/breakout.py
- app/services/signals/evaluators/volume_breakout.py
- app/services/signals/evaluators/volatility_squeeze.py
- app/services/signals/evaluators/support_resistance.py
- app/services/signals/evaluators/advanced.py
- app/services/signals/evaluators/registry.py

Implemented indicator helpers:
- SMA
- EMA
- RSI
- MACD
- Bollinger Bands
- ATR
- percent_change

Implemented evaluators:
- MomentumRateOfChangeEvaluator
- MovingAverageTrendEvaluator
- RsiReversalEvaluator
- MacdCrossoverEvaluator
- MeanReversionEvaluator
- BreakoutPriceThresholdEvaluator
- VolumeConfirmedBreakoutEvaluator
- VolatilitySqueezeEvaluator
- SupportResistanceEvaluator
- VwapReclaimEvaluator
- OpeningRangeBreakoutEvaluator
- RelativeStrengthEvaluator
- TimeSeriesMomentumEvaluator
- MarketRegimeFilterEvaluator
- PairsRelativeValueEvaluator
- OptionsSpreadCandidateEvaluator

Evaluator-backed scanner routing is implemented for:
- momentum_rate_of_change
- moving_average
- rsi_reversal
- macd_crossover
- mean_reversion
- breakout_price_threshold
- volume_confirmed_breakout
- volatility_squeeze
- support_resistance
- vwap_reclaim
- opening_range_breakout
- relative_strength
- time_series_momentum
- market_regime_filter
- pairs_relative_value
- options_spread_candidate

=== CURRENT RECOMMENDED NEXT TASK ===

Next paper-trading tasks:
1. Run controlled paper tests on the five-symbol seeded universe.
2. Compare signal volume and no-signal reasons by scanner type.
3. Tune scanner thresholds and PREVIEW_PROFILE_<PROFILE>_* settings.
4. Improve option contract selection scoring when broker data allows.
5. Keep entries paper-only and human-review strategy changes before changing live logic.

Do NOT let AI or automation directly rewrite strategy logic. AI can recommend changes only.

=== AI REVIEW LAYER STATE ===

Migration 0003 added:
- trade_cases
- ai_trade_reviews
- strategy_change_suggestions

Implemented:
- app/services/trade_cases.py
- post-market maintenance calls trade case population in isolated transaction.
- trade_cases population uses FIFO matched closed round trips and (entry_fill_id, exit_fill_id) dedupe.

Not implemented yet:
- External LLM-backed review generation.
- Automatic application of approved suggestions to strategy config.

Rule:
- AI should recommend changes only, never automatically change live strategy logic.

=== CURRENT KNOWN GAPS ===

- Full evaluator-backed strategy set needs paper-volume tuning and production observation.
- Option contract selection is first-pass and lacks robust delta/Greeks/moneyness/liquidity scoring.
- Broker reconciliation paginates Alpaca FILL account activities with page_token and caps fill_page_size at Alpaca's max of 100.
- News scanning is lightweight RSS/headline gating only.
- Statuses are plain strings, not formal enums/state machine.
- Render cron schedules are UTC-only and must be reviewed around DST.

=== USEFUL LOCAL COMMANDS ===

Run tests:

    python -m pytest tests/services/signals

Run all tests:

    python -m pytest

Check Alembic heads:

    alembic heads

Manual symbol entry job:

    curl -X POST "http://127.0.0.1:8000/api/v1/jobs/market-entry-cycle?symbol=SPY&scan_limit=100&order_limit=100&fill_page_size=100" -H "Authorization: Bearer change-me"

Manual exit cycle:

    curl -X POST "http://127.0.0.1:8000/api/v1/jobs/market-cycle-exits?limit=100&order_limit=100&fill_page_size=100&phase_timeout_seconds=45" -H "Authorization: Bearer change-me"

Automation status:

    curl -H "Authorization: Bearer change-me" "http://127.0.0.1:8000/api/v1/automation/status"

Windows Render health check if certificate revocation check fails:

    curl.exe --ssl-no-revoke -L --max-time 90 https://stocks-api-z11i.onrender.com/health

=== END CURRENT REFRESH ===
